#ifndef TASK_EXAMPLES_H
#define TASK_EXAMPLES_H

/*!
 * @brief Multiply matrix with its own transpose. Resulting matrix should be of size row_count*row_count
 */
void matrix_transpose_multiplication(float matrix[], int row_count, int col_count, float result[]);

#endif